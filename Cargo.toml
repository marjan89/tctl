[package]
name = "tctl"
version = "0.1.0"
edition = "2021"
description = "Test orchestrator for cross-platform device automation"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_yaml = "0.9"
